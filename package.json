{
  "private": true,
  "scripts": {
    "build": "spago build",
    "format": "purs-tidy format-in-place src/**/*.purs",
    "postinstall": "spago install",
    "start": "spago run"
  },
  "devDependencies": {
    "purs-tidy": "^0.7.0"
  },
  "dependencies": {
    "node-fetch": "^2.6.7",
    "purescript": "^0.14.5",
    "spago": "^0.20.3"
  }
}
